# Agent 项目面试题与参考答案（详细版）

> 适用对象：你当前这个「旅行规划 Agent + RAG」项目  
> 使用方式：按“问题 -> 参考答案 -> 可追问点”准备，先背主线，再补细节

---

## 1. 项目定位与价值

### Q1：你这个项目解决了什么业务问题？
**参考答案：**  
这个项目是一个 AI 旅行决策平台，核心是把“检索 + 推理 + 工具调用”串起来，输出可执行行程，而不是只给泛泛建议。  
它主要解决三类问题：
1. 用户需求复杂（预算、天数、同行人、风格、出发地等）时，人工规划成本高。
2. 单纯大模型回答不稳定，缺少实时数据与可解释依据。
3. 传统旅游系统是“信息检索”，缺少“决策编排能力”。

我们通过 RAG 提供知识依据、Agent 工具链补充实时能力（天气、酒店、机票、车票等）、SSE 提供实时交互体验，最终把建议落成可执行方案。

**可追问点：**
1. 你怎么定义“可执行”？
2. 你如何衡量效果提升？

---

### Q2：为什么做成 Agent，而不是普通 RAG 问答？
**参考答案：**  
普通 RAG 擅长“基于知识回答”，但对跨步骤决策不够稳定。旅行规划是多约束优化问题，需要：
1. 先理解用户约束（时间、预算、偏好）。
2. 再按场景调工具（天气/交通/酒店等）。
3. 最后综合输出并给风险与备选方案。

Agent 的价值就在“可编排 + 可追踪 + 可控执行顺序”。我在项目里通过工具轨迹把每一步透明化，便于调优和排障。

---

## 2. 技术架构

### Q3：你项目的整体技术架构是什么？
**参考答案：**  
前后端分离 Monorepo：
1. 前端：Vue3 + TypeScript + Vite + Element Plus，负责参数采集、流式渲染、工具轨迹展示。
2. 后端：Spring Boot 3.4 + Java 17，承担认证授权、RAG、Agent 编排、业务 API。
3. 存储：MySQL（业务数据/会话/知识元数据）、Qdrant（向量检索）、Redis（支撑能力）。
4. 模型接入：BailianClient 统一封装对话、流式、embedding 调用。

架构上是“业务平台层 + 智能编排层 + 知识检索层 + 外部工具层”。

---

### Q4：为什么当前是单体，不直接微服务？
**参考答案：**  
当前阶段优先验证业务闭环和迭代效率，所以选择单体：
1. 部署简单，联调快。
2. 业务改动频繁时，跨服务成本太高。
3. Agent 编排逻辑与工具快速演进，先在一个进程内收敛边界。

但我在代码层面做了“可拆分边界”：`service/agent` 工具独立、RAG 检索独立、模型客户端独立。后续可按运行时/工具网关/知识服务拆分。

---

## 3. Agent 编排与工具治理

### Q5：Agent 的执行流程是什么？
**参考答案：**  
主流程：
1. 规范化用户输入并补结构化字段（出发地、日期、偏好等）。
2. 初始化 `AgentToolRuntime`，按 `@Order` 顺序调度工具。
3. 工具把结果写入 runtime（轨迹、摘要、阻断信息）。
4. 用 `RagLangChainComposer` 组装历史消息 + 检索上下文 + 工具摘要。
5. 调模型生成答案，回写会话并返回轨迹。

这样把“执行”和“生成”分离，稳定性和可观测性会更好。

---

### Q6：你怎么控制工具乱调用或误调用？
**参考答案：**  
我用了三层控制：
1. **触发条件控制**：每个 Tool 的 `shouldRun()` 根据请求参数和结构化上下文判断。
2. **步骤上限控制**：`agent-max-steps` 防止无限扩展调用。
3. **高风险闸门控制**：通过 `HighRiskGateTool` 标记并阻断敏感或高风险路径。

另外所有工具调用都会记录轨迹，便于快速发现误触发并调整规则。

---

### Q7：新加一个工具（比如签证）你怎么做？
**参考答案：**  
标准步骤：
1. 新建 `VisaQueryTool` 实现 `AgentTool` 接口，定义 `toolName/shouldRun/execute`。
2. 新建对应 `VisaLookupService`，封装外部 API 与容错。
3. 在 `AgentChatRequest` 增加必要字段（如 nationality）。
4. 在 `RagAgentService#enrichToolQuestion` 注入结构化键值。
5. 配置 `@Order` 控制执行顺序，并补回归测试。

这个扩展点是插件化的，改动成本可控。

---

## 4. RAG 设计与检索质量

### Q8：你的 RAG 是怎么做的？为什么是混合检索？
**参考答案：**  
我是“向量检索 + 词法检索 + 融合重排”：
1. 向量检索解决语义相似。
2. 词法检索解决关键词精确命中。
3. 融合分数 + 覆盖度重排，平衡语义和关键词。

之所以做混合，是因为纯向量在专有名词和实体词上容易漂移，纯词法又无法处理同义表达。

---

### Q9：你怎么做知识库的多用户隔离？
**参考答案：**  
可见性规则是：
1. 普通用户仅能看到自己上传的文档。
2. 系统共享文档（`createdBy=null`）对普通用户可读。
3. 管理员可看全量文档并执行管理操作。

这一层在检索前过滤 `KnowledgeDocument`，确保召回阶段就不跨越权限边界。

---

### Q10：chunk 策略怎么选的？
**参考答案：**  
当前策略是固定块长 + overlap（配置化），目的是兼顾：
1. 语义完整性（避免句子被截断）。
2. 检索粒度（避免块太大导致噪音）。
3. 向量化成本（块太小会膨胀索引规模）。

参数放在 `app.rag.chunk-size/chunk-overlap`，通过评测脚本做数据驱动调参，而不是拍脑袋。

---

## 5. 外部工具与业务策略

### Q11：机票为什么要做“国内/国外分流”？
**参考答案：**  
因为数据源覆盖与可用性差异很大：
1. 国内场景用户更依赖携程生态，跳转链路更实用。
2. 国外航班保留 aviationstack 查询能力。

我做了路由策略：国内航线走携程，国际航线走 aviationstack，并加了 IATA 解析增强和城市名兜底，减少“无法匹配”的失败率。

---

### Q12：酒店偏好“已预定/安静优先/商圈附近”是怎么落地的？
**参考答案：**  
我把 UI 选择映射到后端执行策略：
1. `已预定`：`HotelQueryTool.shouldRun()` 直接返回 false，跳过酒店工具。
2. `商圈附近`：按距离优先排序。
3. `安静优先`：引入“繁华区域特征词 + 距离区间”打分，按静谧度排序。

这样不是只改文案，而是把业务规则下沉到可执行层。

---

## 6. 安全、权限、合规

### Q13：你怎么做鉴权和访问控制？
**参考答案：**  
采用 Spring Security + JWT：
1. 登录注册放行。
2. `/api/v1/**` 要登录。
3. `/api/admin/**` 要 ADMIN 角色。
4. 结合认证过滤器把用户身份注入上下文。

同时有统一异常处理和 API 访问日志，便于审计。

---

### Q14：你觉得还缺哪些企业级安全能力？
**参考答案：**  
还可以加强：
1. 密钥托管（KMS/Vault），避免明文配置。
2. PII 脱敏与敏感字段分级。
3. 多租户级别配额与隔离策略。
4. 合规审计报表（谁在什么时间访问了什么知识数据）。

---

## 7. 稳定性与故障处理

### Q15：流式 SSE 在生产最容易出什么问题？你怎么处理？
**参考答案：**  
常见问题：
1. 客户端断连导致写入异常。
2. 模型超时或中断。
3. 半包/粘包事件处理错误。

我的处理：
1. 服务端事件发送异常做容错，不让线程二次崩溃。
2. 前端有超时与 abort 控制，可停止生成。
3. 解析逻辑按 event/data 规范拆包，支持 done/error/meta。

---

### Q16：外部工具不稳定时如何降级？
**参考答案：**  
策略是“局部失败不拖垮全局”：
1. 工具失败返回摘要，不抛死全流程。
2. 检索层向量失败时可回退词法检索。
3. 输出里明确“不确定点”和保守建议。

核心目标是：即便部分实时能力失效，也要给用户一份可用方案。

---

## 8. 性能与成本

### Q17：你如何控制大模型成本？
**参考答案：**  
成本主要在三处：对话 token、embedding、工具 API。  
我当前的控制点：
1. 配置 `top-k/context-max-chars`，限制上下文体积。
2. 工具按需触发，避免全量调用。
3. 流式输出减少超时重试带来的浪费。
4. 预留 fallback model 机制，做性能/质量平衡。

下一步可以加：Prompt 缓存、语义缓存、分层模型路由。

---

### Q18：性能瓶颈通常在哪？
**参考答案：**  
通常有四类瓶颈：
1. 外部 API RTT（天气/酒店/机票）。
2. 向量检索 + 反查元数据。
3. LLM 推理时间。
4. 前端渲染长文本和轨迹列表。

定位时我会按“网关耗时 -> 工具耗时 -> 模型耗时 -> 前端渲染耗时”分段看。

---

## 9. 可观测与调试

### Q19：如何排查“回答不靠谱”？
**参考答案：**  
我会按链路拆：
1. 看工具轨迹：是否该调用没调用、是否调用顺序错。
2. 看 references：召回文档是否偏题。
3. 看提示词拼装：系统指令是否覆盖工具结论。
4. 看最终回答：是否被模型“改写过度”。

这也是我坚持输出 tool trace 的原因，排障效率明显更高。

---

### Q20：你会看哪些关键指标？
**参考答案：**  
我会重点看：
1. 首 token 时间、总响应时延、P95/P99。
2. 工具成功率、工具超时率、工具平均耗时。
3. 检索命中率、引用覆盖率。
4. 会话失败率、重试率。
5. 单请求 token 成本、日成本趋势。

---

## 10. 工程化与交付

### Q21：你的测试策略是什么？
**参考答案：**  
分三层：
1. 单测：工具规则、解析函数、排序逻辑。
2. 集成：Controller -> Service -> Repo 主链路。
3. 回归：固定问答集做 RAG/Agent 质量对比（项目已有评测脚本）。

每次改工具规则（如酒店偏好）都要补回归，防止旧行为回退。

---

### Q22：上线怎么做灰度与回滚？
**参考答案：**  
建议流程：
1. 新增配置开关（按功能控制）。
2. 先给小流量用户打开。
3. 观察错误率、时延、用户反馈。
4. 异常时立即关开关回滚逻辑。

由于项目配置项比较完善，很多能力可以“配置回滚”而不是“代码回滚”。

---

## 11. 典型业务改动题（高频面试追问）

### Q23：你最近做过的一个改动，如何保证不影响其他功能？
**参考答案：**  
可以讲“酒店偏好三态改造”：
1. 前端：布尔改三态枚举，不改其他字段语义。
2. 后端：新增 `hotelPreference` 字段并保留 `hotelRecommendation` 兼容。
3. 工具层：仅在 `shouldRun` 和排序逻辑做局部改动。
4. 验证：前后端都编译验证，手工走“已预定/安静优先/商圈附近”三条路径。

这属于“向后兼容 + 小步快跑”的改法。

---

### Q24：如果让你把这个项目提升到企业级，优先做什么？
**参考答案：**  
我会按优先级做三批：
1. **稳定性优先**：异步任务化、重试熔断、统一错误码、SLO看板。
2. **治理优先**：模型与 Prompt 版本管理、评测门禁、灰度发布。
3. **合规优先**：多租户隔离、密钥托管、审计报表、数据生命周期策略。

先把“能跑”升级为“可运营”，再做“可规模化”。

---

## 12. 面试收尾题

### Q25：你在这个项目里最核心的技术贡献是什么？
**参考答案：**  
我会强调三点：
1. 把系统从“单纯问答”推进到“可编排 Agent”。
2. 把结果从“黑盒回答”升级为“可追踪执行链路”。
3. 把业务需求（机票分流、酒店偏好）沉淀成可维护规则，而不是一次性硬编码。

这三点体现的是工程能力，而不只是调用模型 API。

---

### Q26：你做得不够好的地方是什么？准备怎么改？
**参考答案：**  
目前短板是：
1. 压测与容量模型还不够系统化。
2. 评测集覆盖线上真实长尾场景还不够。
3. 成本可视化还没做成日报表级别。

改进计划：
1. 引入标准压测场景和阈值告警。
2. 建立线上问题回灌评测集机制。
3. 增加 token/工具调用成本看板并绑定发布门禁。

---

## 13. 你的答题模板（建议背）

### 模板 A：技术选型题
1. 先说业务约束。  
2. 再说为什么这个选型最匹配当前阶段。  
3. 最后说替代方案和后续演进路径。  

### 模板 B：稳定性题
1. 先列失败模式。  
2. 再说当前防护。  
3. 最后说未完成项和落地计划。  

### 模板 C：复杂改造题
1. 先讲目标与边界。  
2. 再讲最小改动路径。  
3. 最后讲验证矩阵和回滚策略。  

---

## 14. 快速自测清单（面前 10 分钟看）

1. 你能在 60 秒讲清楚系统架构吗？  
2. 你能在 90 秒讲清楚 Agent 执行链吗？  
3. 你能解释一次“线上故障定位路径”吗？  
4. 你能说清楚最近一个需求是如何兼容落地的吗？  
5. 你能回答“为什么不是微服务/为什么不是纯RAG”吗？  
6. 你能讲出下一阶段三件最重要的工程动作吗？

---

如果你要，我可以再给你一版“**压力面试版**”（每题带反问和陷阱），专门训练高压场景答题。

